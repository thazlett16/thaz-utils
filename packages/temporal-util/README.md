# thazstack temporal-util

Until the Temporal API is available more widely this code relies on the official polyfill as a peerDependency.

The goal of this package is standardizing the formatter with some default options and exporting some resolved options for client side work.

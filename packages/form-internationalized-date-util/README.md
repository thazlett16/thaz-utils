# thazstack form-util

Until the Temporal API is available more widely this code relies on the official polyfill as a peerDependency.

This package is an extension of `@thazstack/form-util`. This is mostly to update a few hooks and data types since some component libraries need a different value in the component. One example is react-aria

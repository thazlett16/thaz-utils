# thazstack form-util

Until the Temporal API is available more widely this code relies on the official polyfill as a peerDependency.

The goal of this package is standardizing my applications to use `@tanstack/react-form` and the temporal API. This acts as utility schema in my form as well as mapping state types to a normalized type for consumption in form components.

{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "paths": {
      "#src/*": ["${configDir}/src/*"],
      "#test/*": ["${configDir}/test/*"],
      "#mock/*": ["${configDir}/mock/*"]
    }
  }
}

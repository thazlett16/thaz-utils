# thazstack Oxlint setup

I recently converted to Oxlint as my lint tool of choice. This is a WIP but I like to have lots of rules to keep the codebase pretty tight. Might tweak though as time goes on.
